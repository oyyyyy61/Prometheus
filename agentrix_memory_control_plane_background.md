# Agentrix 长程 Agent 内存可观测与控制系统：设计背景与动机

## 1. 背景

随着大语言模型从单轮问答逐步演进到具备规划、工具调用、反思、分支探索和长期执行能力的 Agent，推理系统面临的资源管理问题也发生了显著变化。

传统大模型推理服务通常围绕“请求”进行资源管理。一次请求到达后，系统完成 Prefill 和 Decode，生成结束后释放相关运行时状态。这种模式下，内存管理的主要目标是提高单次请求或批量请求的吞吐、降低延迟，并尽可能提高 KV Cache 的利用率。

长程 Agent 的执行方式与此不同。一个 Agent 任务可能持续数分钟甚至更长，并在执行过程中不断经历以下状态：

- 长上下文持续增长；
- 多轮工具调用与外部环境交互；
- 工具执行期间模型暂停而任务仍保持存活；
- 为搜索、验证或规划产生多个并行分支；
- 多个分支共享大量历史上下文；
- 中间结果被阶段性使用后失去价值；
- 部分状态需要长期保留，部分状态可以迁移、回收或重建；
- Agent 在 CPU、NPU/GPU、主存和外部存储之间形成跨层资源依赖。

因此，Agent 的资源消耗不再只是一次推理请求的瞬时开销，而逐渐演变为一个具有**生命周期、依赖关系、共享关系和状态迁移**的长期资源管理问题。

在这一背景下，仅观察设备总体显存或 KV Cache 利用率已经难以解释系统行为，更无法回答长程 Agent 场景中最关键的问题：

> 当前内存究竟被哪些 Agent、哪些分支和哪些历史状态占用？  
> 这些状态为什么仍然存活？  
> 哪些内存正在被使用，哪些只是等待未来可能再次访问？  
> 哪些数据可以安全回收、迁移或降级存储？  
> 一次人工干预会释放多少资源，又会带来多大的恢复或重计算代价？

这些问题构成了设计 Agentrix 长程 Agent 内存可观测与控制系统的直接背景。

---

## 2. 现有监控方式的局限

当前推理系统通常已经能够提供部分基础运行指标，例如：

- 设备显存占用；
- KV Cache 使用率；
- 当前运行请求数量；
- 等待请求数量；
- Prefix Cache 命中率；
- 请求延迟与吞吐；
- CPU 内存、磁盘或网络流量。

这些指标对于传统在线推理非常重要，但在长程 Agent 场景下仍然存在明显不足。

### 2.1 缺少 Agent 语义

设备可以显示“当前 NPU 内存使用率为 82%”，但这个数字无法说明：

- 哪个 Agent 占用了这些内存；
- 一个 Agent 内部的哪个分支占用了这些资源；
- 某段 KV Cache 对应的是共享历史还是私有后缀；
- 内存是否属于正在运行的推理，还是属于等待工具返回的空闲状态；
- 某段数据是否还会被后续阶段继续使用。

因此，传统监控描述的是**设备状态**，而不是**Agent 状态**。

### 2.2 缺少共享关系

长程 Agent 常常包含树状或图状执行结构。

多个分支可能共享相同父历史，因此“每个分支的逻辑内存大小”并不等于“设备实际消耗的物理内存”。

例如，四个分支在逻辑上可能各自拥有 1 GiB 历史，但其中 800 MiB 是共同共享的物理前缀。此时：

- 逻辑总量可能接近 4 GiB；
- 实际物理占用可能远小于 4 GiB；
- 如果仅对每个分支分别统计，很容易重复计算；
- 如果只看设备总量，又无法理解共享带来的收益。

因此，系统需要同时表达**逻辑资源视图**和**物理资源视图**。

### 2.3 缺少生命周期信息

对于长程任务而言，“一块内存仍然存在”并不意味着“它当前有计算价值”。

典型例子是工具调用：

```text
模型执行
  ↓
发起工具调用
  ↓
等待文件系统、搜索、编译、测试或网络服务
  ↓
工具返回
  ↓
恢复模型执行
```

在等待期间，一些模型状态可能继续驻留在高价值设备内存中，但此时模型并没有使用它们进行计算。

同样地，某些阶段性中间结果已经被所有消费者使用完毕，却可能仍然保留到整个 Session 结束。

因此，长程 Agent 的核心问题之一不是“是否分配了内存”，而是：

> **这份内存当前处于生命周期中的什么位置？**

如果系统缺少这一层信息，就很难判断资源是否真正有必要继续驻留。

### 2.4 缺少干预能力

传统监控系统通常以“观察”为主。

当用户发现：

- 某个 Agent 长时间等待工具；
- 某个分支已经不再重要；
- 某段历史占用大量设备 KV；
- 某个阶段产生大量中间数据；
- 系统正接近设备内存压力上限；

用户往往只能选择终止整个任务，或者等待自动策略处理。

然而，在研究、调试、资源紧张或交互式运行环境中，用户可能希望进行更细粒度的干预，例如：

- 保留某个重要 Agent 的高价值状态；
- 允许某些低优先级状态被回收；
- 将部分状态迁移到较低层级的存储；
- 提前恢复即将再次使用的状态；
- 主动结束已经无价值的分支；
- 在系统执行动作前查看可能带来的资源收益与性能代价。

因此，仅有可观测性仍然不足，还需要进一步探索一种**面向 Agent 语义的受控管理能力**。

---

## 3. 核心设计动机

本系统的设计动机并不是再实现一个通用显存监控面板，而是希望解决长程 Agent 资源管理中的“不可解释”和“不可干预”问题。

核心目标可以概括为：

> **让用户能够理解每一份重要内存由谁占用、为什么仍然存在、与哪些 Agent 状态相关，并在明确资源关系和潜在代价的前提下进行受约束的人工管理。**

这一目标可以进一步拆分为以下几个设计动机。

---

## 4. 动机一：从设备视角转向 Agent 视角

传统系统通常以设备为中心：

```text
NPU 0
├─ 已使用显存
├─ KV Cache
├─ 模型权重
└─ 临时工作区
```

对于 Agent 系统，更有意义的视角可能是：

```text
Agent A
├─ Shared History
├─ Branch 1
│  ├─ Private KV
│  └─ Tool State
├─ Branch 2
│  ├─ Private KV
│  └─ Tool State
└─ Intermediate Data
```

设备内存依然重要，但它应该能够被映射回 Agent 的执行结构。

只有建立这种映射，用户才能理解：

- 为什么内存增长；
- 为什么一个任务结束后内存没有立刻下降；
- 哪个分支造成了内存峰值；
- 哪些资源实际上被多个分支共享；
- 哪些资源已经成为长期驻留的“冷状态”。

因此，本系统首先需要建立**资源与 Agent 执行结构之间的关联**。

---

## 5. 动机二：区分逻辑内存与物理内存

Agent 分支结构天然包含共享。

如果只展示逻辑上下文大小，可能严重高估物理成本；如果只展示物理内存，又无法表达共享机制到底节省了多少资源。

因此，监控系统应该能够同时回答：

### 逻辑视角

如果每个分支独立保存自己的历史，需要多少资源？

### 物理视角

在 Prefix Sharing、Copy-on-Write、Paged KV 等机制下，设备实际保存了多少数据？

### 共享视角

当前物理资源中：

- 哪些由多个分支共享；
- 哪些由单一分支独占；
- 哪些仍有多个消费者；
- 哪些已经只剩最后一个引用。

这种逻辑—物理双视角不仅可以帮助用户理解内存，还可以成为评估 Agentrix 各类共享优化是否真正有效的重要工具。

---

## 6. 动机三：把“生命周期”作为一等资源属性

在传统请求模型中，资源通常只有“使用中”和“已释放”两个主要阶段。

长程 Agent 更复杂。

一份状态可能经历：

```text
创建
  ↓
活跃使用
  ↓
共享
  ↓
等待
  ↓
降温
  ↓
迁移
  ↓
恢复
  ↓
再次使用
  ↓
最终释放
```

不同状态还可能具有完全不同的生命周期。

例如：

- KV Cache 可能在设备、主存和外部存储之间迁移；
- 工具输出可能在模型外长期保存，仅按需重新读取；
- 分支共享历史可能在多个消费者结束后逐步失去价值；
- 某些阶段结果可能在 workflow checkpoint 后立即不再需要；
- 某些资源虽然当前未被使用，但即将在下一步恢复。

因此，系统需要能够表达：

> **资源不仅有“大小”和“位置”，还有“状态”和“未来价值”。**

这也是长程 Agent 内存管理区别于普通大模型 serving 内存管理的重要特征。

---

## 7. 动机四：识别真正可回收的资源

“已占用”不等于“可释放”。

一个看起来处于空闲状态的 KV Block，可能：

- 正被另一个分支共享；
- 正在参与异步传输；
- 正被某次 Attention 使用；
- 已被下层存储备份；
- 没有任何副本，释放后只能重新计算；
- 即将在短时间内恢复使用。

因此，本系统需要特别关注**Reclaimable Memory（可回收内存）**这一概念。

相比简单显示：

```text
NPU KV = 7.4 GiB
```

更有价值的信息是：

```text
NPU KV               7.4 GiB
Currently Active      3.9 GiB
Shared / Referenced   1.6 GiB
Potential Reclaim     1.9 GiB
```

其中“可回收”不应该仅仅依据“最近没有访问”，而应结合 Agent 结构和当前生命周期进行解释。

这一能力既服务于用户，也可以服务于系统自动策略的开发和验证。

---

## 8. 动机五：支持人在回路中的资源管理

长程 Agent 在实际使用中可能运行在不同场景：

- 研究实验；
- 本地工作站；
- 单卡资源受限环境；
- 多用户共享服务器；
- 长时间 Coding Agent；
- 大规模并发 Agent 服务。

在这些环境下，用户对资源的偏好并不总是相同。

例如：

- 一个重要任务可能更关注恢复延迟，希望尽量保留热状态；
- 一个低优先级任务可能允许重新计算，以换取更多设备容量；
- 在显存紧张时，用户可能愿意主动迁移等待中的 Agent；
- 在 Debug 场景下，用户可能希望固定某些状态，观察其他资源变化；
- 在测试某种自动策略时，研究者可能希望手动覆盖策略决策。

因此，系统应探索一种**Human-in-the-loop Memory Management** 模式：

系统负责维护底层资源正确性和安全边界，用户表达高层资源管理意图。

用户不应该直接操作底层物理 Block，而应该从 Agent、Branch、Session、Stage 等语义对象出发进行管理。

具体控制方式仍有待进一步研究，本阶段不对最终机制做预设。

---

## 9. 动机六：为 Ascend 与异构内存优化提供统一观测基础

Agentrix 正在探索 Ascend NPU 环境下的 Agent 推理优化。

在异构平台上，资源管理问题会进一步复杂化：

- NPU HBM 容量有限但访问速度最高；
- CPU DDR 可作为更大容量的下层存储；
- SSD 或远端内存可承担更冷的数据；
- H2D / D2H 迁移存在额外延迟和带宽竞争；
- ACLGraph 等执行机制会对运行时状态和工作区提出额外约束；
- Attention、KV restore 和通信可能共同竞争设备带宽。

因此，仅观察 NPU 总内存并不能判断一个 offload 或 restore 策略是否合理。

理想的监控系统需要帮助回答：

- 当前有多少 KV 位于 NPU；
- 有多少已经存在 CPU 或外部副本；
- 正在发生多少 D2H / H2D 数据迁移；
- 哪些 Agent 正在等待恢复；
- 某次迁移是否真正降低了设备工作集；
- 释放出的容量是否转化为了更高并发；
- 恢复代价是否抵消了释放带来的收益。

因此，该系统也可以成为 Agentrix Ascend 优化的统一实验与验证基础设施。

---

## 10. 动机七：把系统优化从“黑盒结果”变成“可解释过程”

如果一个优化最终只得到如下结果：

```text
Baseline: 10.2 s
Agentrix: 8.1 s
```

很难解释性能变化来自哪里。

对于复杂 Agent workflow，整体性能可能同时受到：

- 上下文长度；
- KV Cache；
- Prefix Sharing；
- 分支数；
- Tool Wait；
- Offload；
- Restore；
- Attention Backend；
- 调度；
- 数据迁移；

等多个因素影响。

因此，需要一种方式观察完整时间线。

例如：

```text
Planning
    ↓
Fork
    ↓
Parallel Decode
    ↓
Tool Wait
    ↓
KV Pressure
    ↓
State Transition
    ↓
Tool Return
    ↓
Restore
    ↓
Merge
```

并将资源状态与这些事件对齐。

这样既可以解释优化结果，也可以快速发现性能异常，例如：

- 某个分支意外保留了大量状态；
- 某次 offload 后很快又立即 restore；
- ForkAttention 的共享条件未实际形成；
- 某个 tool wait 阶段存在明显内存白占；
- 某个 workflow stage 已结束但中间结果仍未回收。

因此，本系统同时具有**产品可观测性**与**系统研究 instrumentation** 的双重价值。

---

## 11. 期望形成的能力

在不预设最终实现方案的前提下，本系统希望最终能够支持以下几类能力。

### 11.1 资源归因

能够将重要资源关联到：

- Agent；
- Session；
- Branch；
- Workflow Stage；
- Shared Prefix；
- Tool Execution；
- Intermediate Data。

### 11.2 生命周期观测

能够观察关键资源从创建到释放过程中经历的状态变化。

### 11.3 多层内存视图

能够区分并观察：

- NPU/GPU Device Memory；
- CPU Host Memory；
- Local Storage；
- Remote / Distributed Storage。

### 11.4 共享关系表达

能够表示多个 Agent 分支之间对同一资源的共享和引用关系。

### 11.5 历史时间线

能够将 Agent 行为事件与内存变化进行关联，帮助解释资源峰值与异常。

### 11.6 潜在资源管理入口

在确保底层系统正确性的前提下，为用户提供面向 Agent 语义的资源管理能力。

具体动作、策略和执行接口仍需结合 Agentrix 当前的 KV 管理、Ascend 执行路径以及实验结果进一步确定。

---

## 12. 非目标

为了避免将系统演化成一个泛化基础设施项目，本阶段不以以下目标为重点：

- 替代 Prometheus、Grafana 等通用监控系统；
- 替代 NPU/GPU 驱动级性能分析工具；
- 提供任意物理地址或 KV Block 的直接修改能力；
- 在尚未验证安全性的情况下允许用户强制释放底层内存；
- 一开始覆盖所有模型、所有推理框架和所有异构硬件；
- 在缺少实验依据时提前固定自动内存管理策略。

系统的核心仍然是：

> **围绕长程 Agent 的生命周期和执行结构，建立具有 Agent 语义的内存可观测与控制能力。**

---

## 13. 与 Agentrix 整体方向的关系

Agentrix 当前关注的是长程 Agent 场景中的内存与执行效率问题。

从整体上，可以将未来能力划分为三个相互补充的方向：

### 计算侧

探索多分支 Agent 场景下共享上下文带来的计算与内存访问优化，例如共享 Prefix 的 Attention 执行优化。

### 内存侧

探索 Agent 生命周期驱动的 KV Cache、工具数据和中间状态管理，以及多层存储之间的迁移和恢复。

### 控制与观测侧

提供统一的 Agent-aware Memory Observability & Control Plane，使上述自动优化的行为能够被理解、验证、调试，并在必要时允许用户进行受控干预。

三者之间的关系可以抽象为：

```text
                Long-running Agent
                       │
           ┌───────────┼───────────┐
           │           │           │
        Compute      Memory      Control
           │           │           │
     Execution Opt   Lifecycle   Observe / Manage
           │           │           │
           └───────────┼───────────┘
                       │
                 Agentrix Runtime
                       │
                Ascend / CUDA
```

其中，本设计重点讨论第三部分。

它并不独立于 Agentrix 的其他优化存在，而是希望成为连接 Agent workflow、底层推理运行时和异构存储层之间的统一可观测与控制入口。

---

## 14. 当前阶段的开放问题

本设计目前仍处于问题定义和系统边界阶段，具体方法不做定论。

后续需要进一步研究的问题包括：

1. 如何定义稳定且低开销的 Agent—资源关联模型；
2. 如何处理多个 Branch 对同一物理 KV 的共享归因；
3. 如何准确计算“可回收内存”，而不将逻辑引用误认为物理占用；
4. 如何描述资源的生命周期状态和状态转移；
5. 如何在不明显影响推理性能的前提下采集细粒度事件；
6. 如何将应用层 Tool 生命周期与 vLLM / vLLM-Ascend 的底层资源状态对齐；
7. 哪些用户控制操作具有足够清晰的语义和安全边界；
8. 是否需要为人工操作提供预估收益、恢复成本和风险提示；
9. 如何记录人工干预，使实验能够完整复现；
10. 如何将该系统用于评价自动内存管理策略，而不是仅作为展示界面；
11. 如何在单 NPU 验证完成后扩展到 TP、DP、多实例和远端 KV 存储；
12. 哪些指标最能够反映长程 Agent 的实际资源效率。

这些问题将在后续原型、真实 Agent workload 和 Ascend 实验的基础上逐步收敛。

---

## 15. 总结

长程 Agent 将推理系统中的内存问题从一次请求内部的缓存管理，扩展成了一个跨越分钟级生命周期、分支执行、工具等待、共享历史和异构存储的资源管理问题。

传统设备级监控虽然能够展示总体利用率，却难以解释资源与 Agent 行为之间的关系，也无法支持面向 Agent 语义的资源管理。

因此，Agentrix 希望探索一种面向长程 Agent 的 Memory Observability & Control Plane，使系统能够：

- 观察资源；
- 理解归属；
- 表达共享；
- 跟踪生命周期；
- 分析潜在可回收空间；
- 解释系统优化效果；
- 为未来安全、受约束的人工资源管理提供基础。

在当前阶段，本设计重点明确问题、动机和系统边界，具体技术方法与控制策略仍保持开放，将以后续真实 workload、Ascend 实验和 Agentrix 运行时能力为依据逐步确定。
